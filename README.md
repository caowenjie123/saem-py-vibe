# saemix Python Implementation

Python implementation of the SAEM (Stochastic Approximation Expectation Maximization) algorithm for nonlinear mixed effects models.

## Installation

```bash
pip install -r requirements.txt
pip install -e .
```

## Usage

See `examples/basic_example.py` for a basic usage example.

## Status

This is a work in progress. Core functionality is being implemented.